
<article>

<div>
    <ul class="list-group">
        {{#each post }}
      <li>
        {{> post-info post}}  
      </li>
        {{/each}}

    </ul>
</div>


</article>

{{!-- <article>
    <h2>{{post.title}}</h2>
    <p>{{post.post_content}}</p>
    <a href= "/post{{post.id}}"> {{post.comments.length}} comment(s)</a>
</article> --}}

